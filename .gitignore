# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.idea/
*.swp
*.swo
*~

# VS Code / Cursor project settings (tracked)
# Keep shared project settings in git; ignore only local overrides.
.vscode/settings.local.json
.vscode/*.local.json

# Logs
*.log
logs/

# Config files (may contain secrets)
cacherr.json
mcp.json
.env
.env.*
*.env
.env.local
.env.*.local

# Allow checked-in templates
!.env.example

# Cursor MCP config often contains API keys/tokens. Keep it local.
.cursor/mcp.json

# Claude Code local overrides (personal prefs, experiments, secrets)
.claude/settings.local.json
.claude/.credentials.json

# Docker
.docker/

# Node.js (frontend)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
frontend/dist/
frontend/build/

# OS
.DS_Store
Thumbs.db

# Lock files (runtime)
*.lock
!requirements*.txt
!package-lock.json
!yarn.lock

# Test coverage
.coverage
htmlcov/
.pytest_cache/

# Local DB files
*.db
*.sqlite
*.sqlite3

# Tracker files (runtime data)
*.plexcached
cache_timestamps.json
watchlist_tracker.json
ondeck_tracker.json
.claude/_state/

# Cache directories
.cache/
.npm/
